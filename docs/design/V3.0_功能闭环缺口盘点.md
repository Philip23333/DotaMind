# V3.0 功能闭环缺口盘点

> 目标:V3.0 = "基础的完整功能闭环"。本文盘点当前工具/契约覆盖了哪些查询、哪些常见查询**接不上**(planner 只能回 `insufficient_tools`),并给出 V3.0 工具路线图与首个切片建议。
>
> "闭环"定义:用户查询 → planner 能选出工具 → 工具产出 evidence → critic 通过 → answer。任何一环缺工具 = 缺口。

## 1. 当前能力地图(v2.5 收尾态)

### 工具(16 个,4 个数据源)

| 数据源 | 工具 | 产出 evidence kind |
|---|---|---|
| STRATZ | `resolve_hero` | hero_identity |
| STRATZ | `stratz.pair_lane_outcome` | pair_lane_winrate, sample_size |
| STRATZ | `stratz.hero_matchup_ranking` | matchup_ranking_row, sample_size (+ candidate_rows) |
| STRATZ | `stratz.hero_synergy_ranking` | hero_synergy_ranking_row, sample_size (+ candidate_rows) |
| STRATZ | `stratz.lane_meta_global` | lane_meta_row, sample_size |
| STRATZ | `stratz.hero_position_stats` | position_stat, sample_size |
| STRATZ | `stratz.hero_daily_trends` | hero_daily_trend |
| STRATZ | `stratz.filter_heroes_by_position` | role_filtered_candidate_row |
| OpenDota | `opendota.resolve_team` | team_identity (+ team_id) |
| OpenDota | `opendota.team_recent_matches` | recent_matches, sample_size (+ matches) |
| OpenDota | `opendota.team_players` | current_players |
| OpenDota | `opendota.team_heroes` | team_hero_usage, match_detail_sample, sample_size |
| OpenDota | `opendota.hero_stats_by_role` | hero_stats, role_fit, sample_size |
| Local patch | `patch.get_records` | patch_records, patch_buff/nerf_count |
| Local patch | `patch.hero_changes` | hero_patch_changes |
| Local patch | `patch.item_changes` | item_patch_changes |

### 契约(4 个)
`natural_language_answer`(自由回答)、`patch_impact_report`、`role_meta_report`、`team_recent_report`(后 3 个结构化)。

### 已闭环的查询类型
英雄克制、队友配合、对线胜率/碾压、强势/常见对线组合、位置强度、位置过滤候选、胜率日趋势、英雄辨识、版本改动、战队近况、角色 meta。

## 2. 缺口分析(按"接不上"严重度排序)

### 🔴 P0 — 高频核心缺口(直接对应 V3.0 用户蓝图)

**G1. 英雄出装攻略(item / talent / skill build)**
- 查询样例:"Lina 出什么装?"、"PA 几号位核心装 + 天赋怎么点"、"冰魂辅助装流派"。
- 现状:**无任何工具**。`patch.item_changes` 只讲版本改动,不是 build。
- 需要的 evidence:`item_build`(核心/ situational / 起手 / 中立)、`talent_build`、`skill_build`。
- 数据源可行性:**STRATZ 可行,需 schema 探查**。`stratz_schema_reference.md` 目前只覆盖 heroStats/laneOutcome/matchup/stats(我们已用的);build 数据(物品流行度/ timings)在 2.1MB introspection 里但未定位具体 query。需要先跑 `apps/api/scripts/stratz_schema_docs.py` 找到 item-build 入口(`heroStats.itemBuild` / `itemPopularity` / item timings 之类)。
- 复杂度:中(需新 STRATZ query + 集成层 + agentic 工具 + evidence-first 透传)。

**G2. 玩家战绩查询(player profile + recent matches + hero usage)**
- 查询样例:"查我的战绩"、"这个 ID 最近玩什么英雄胜率高"、"某玩家近 20 场战绩"。
- 现状:**无玩家级工具**(只有 team 级)。`integrations/opendota/` 只有 heroes/teams,**无 players.py**。
- 需要的 evidence:`player_identity`、`player_recent_matches`、`player_hero_usage`、`player_win_loss`。
- 数据源可行性:**OpenDota 公共 API 支持**( `/players/{account_id}`、`/players/{account_id}/recentMatches`、`/players/{account_id}/heroes`、`/players/{account_id}/wl`、`/search?q=` ),但**全是绿地集成代码**:需新建 `integrations/opendota/players.py` + `resolve_player` 解析器 + 2~4 个工具。
- 前置子缺口:**玩家名/SteamID → account_id 解析**(类比 `resolve_hero`/`resolve_team`)。OpenDota `/search` 可做。
- 复杂度:中偏高(解析 + 集成层 + 多工具)。

### 🟡 P1 — 中频,补上后"闭环感"显著提升

**G3. ban/pick 率 meta(选取/禁用热度)**
- 查询样例:"这版本什么英雄非 ban 必选?"、"Lina 出场率多高"。
- 现状:**部分**。STRATZ heroStats 暴露 pickCount/banCount,但 `stratz.hero_position_stats` 只透传 matchCount/winCount,没带 ban/pick。前一个用例的回答里就主动声明了"未涉及禁用率/选取率"。
- 方案:扩 `hero_position_stats` 的 value 加 ban/pick 字段(evidence-first 加列,非新工具),或新 `stratz.hero_pick_ban_meta`。
- 复杂度:低(主要是集成层补字段 + evidence 透传)。

**G4. 单局详情(match detail)**
- 查询样例:"这场 XXX 的出装/经济曲线"、"match_id=... 的细节"。
- 现状:**集成层已存在**(`OpenDotaTeams.get_match_detail(match_id)` 已实现,`team_report.match_details` policy 也有),但**没暴露成 agentic 工具**。
- 方案:包一层 `opendota.match_detail` 工具 + `match_detail` evidence。低 effort。
- 复杂度:低(集成层现成,只差工具壳 + evidence)。

### 🟢 P2 — 低频或高复杂,排到 V3.x

- **G5. 阵容评分(5v5 draft evaluation)**:需要 pairwise synergy+counter 矩阵组合成 5v5 分。和"thin relay、不打综合分"原则冲突(会发明聚合分)。复杂,留 V3.x+,需单独设计。
- **G6. 物品 meta(item-centric win rates)**:"这版本什么装备强"。可从 match 数据派生。低优先。
- **G7. 职业赛事(tournament/series)**:有 team 工具但无赛事/联赛工具。可能超出"基础闭环"范围。

## 3. V3.0 工具路线图(建议优先级)

| 优先级 | 缺口 | 切片 | 依赖/前置 |
|---|---|---|---|
| **P0-a** | G1 出装攻略 | STRATZ item-build schema 探查 → 集成层 query → `stratz.hero_build_guide` 工具(含 item/talent/skill) | schema 探查(`stratz_schema_docs.py`) |
| **P0-b** | G2 玩家战绩 | `integrations/opendota/players.py` + `resolve_player` + `opendota.player_recent_matches` + `opendota.player_heroes` | 玩家解析(OpenDota `/search`) |
| **P1** | G4 单局详情 | `opendota.match_detail` 工具(集成层现成) | 无 |
| **P1** | G3 ban/pick | 扩 `hero_position_stats` 透传 ban/pick 字段 | 无 |
| **P2** | G5/G6/G7 | 后续 | — |

## 4. 跨切面基础(建议与新工具并行/前置)

- **A. §7.2 critic→planner retry 回环(`max_replans=1`)**:新工具(G2 玩家战绩尤其)容易产出稀疏 evidence(玩家近期场次少);retry 回环让 critic 判稀疏后能让 planner 补工具/放宽阈值再跑。这是 V3.1 critic 优化的前置,但对新工具的健壮性起底——建议**与 P0 并行或紧跟其后**。见 v2.5 设计 §7.2 + Sample Policy 阶段 2。
- **B. Prompt 收敛(暂停中)**:加 2~4 个新工具 = planner prompt 更长;先收敛到 `app/agentic/prompts/` 能让增量更可控。非阻塞,但建议在新工具上线前做。
- **C. `resolve_player` 解析器**:和 `resolve_hero`/`resolve_team` 同构,是 G2 的硬前置,也是一个干净的独立切片。

## 5. 建议的 V3.0 首个切片

**推荐:P0-b 玩家战绩(G2)** 作为第一刀,理由:
1. 数据源确定(OpenDota 公共 API,无需 schema 探查),路径清晰、风险低于 G1(后者要先摸 STRATZ item-build schema)。
2. 带 `resolve_player` 解析器——能立刻复用并验证现有 `resolve_hero`/`resolve_team` 的解析器模式。
3. 用户价值高、查询频次高。

替代选项:若想先拿"低 effort 快胜",可先做 **G4 单局详情**(集成层现成,最快出成果),再上 G2。

不做第一刀的:G1 出装攻略—— worthwhile,但需要先跑 schema 探查定位 STRATZ item-build 入口,不确定性更高,适合作为第二刀(探查完再设计)。
