# DotaMind 进度快照：2026-06-23 14:05

> 本文记录 Critic 质量门第三步：质量阈值迁移到统一 `policy.yaml`。

## 本次更新

- `apps/api/app/config/policy.yaml` 的 `critic` section 新增质量门配置：
  - `mock_allowed`
  - `min_confidence`
  - `hard_min_confidence`
  - `team_report.max_latest_match_age_days`
  - `team_report.hard_max_latest_match_age_days`
  - `team_report.min_matches_in_window`
  - `team_report.min_match_details_analyzed`
- `apps/api/app/core/config.py` 新增 Critic 质量门 Pydantic 模型和校验。
- `CriticAgent` 不再使用硬编码阈值，改为读取 `get_policy().critic`。
- `docs/technical/configuration.md` 同步说明 Critic policy 已覆盖 mock、confidence、freshness 和 sample-size 质量门。

## 校验规则

- `hard_min_confidence` 不能大于 `min_confidence`。
- `team_report.max_latest_match_age_days` 不能大于 `team_report.hard_max_latest_match_age_days`。
- 所有 Critic 数值阈值都有范围约束。
- Policy 仍禁止未知字段。

## 当前边界

- API 行为仍保持不变：Critic 的 warning/failed 只进入 `tasks` trace，不改变 HTTP status。
- 下一步如果要启用阻断，需要在 route/service 层将 `quality gate failed` 映射为 `422` 或结构化 `quality_failure` 响应。

## 验证

```text
python -m pytest
55 passed, 3 skipped, 1 warning

python -m ruff check app tests
All checks passed!
```

