# DotaMind Progress Snapshot: 2026-07-15 15:43

> This snapshot records documentation-structure consolidation on
> `feature/v3-functional-loop`. This phase changes documentation only; runtime
> code is unchanged.

## Documentation Entry Point

- Added `docs/README.md` to define the reading order and authority levels:
  - `DotaMind_V3.0_design.md` is the current product and capability design.
  - `DotaMind_MVP_v2.5.md` is the architecture foundation V3 must preserve.
  - `technical/architecture.md` is the current code and runtime map.
- Updated the root `README.md` to DotaMind V3 wording and linked the new index.
- Aligned `apps/api/README.md` with the current API, contract registry, and tool
  registry.

## Current Code Facts

Read-only checks against the current tree established the following wording:

- Active endpoints: `GET /health`, `POST /api/v1/plan`, and `GET /debug/plan`.
- Four output contracts:
  - `natural_language_answer`
  - `patch_impact_report`
  - `role_meta_report`
  - `team_recent_report`
- The agentic registry exposes 19 tools across local hero constants, STRATZ,
  OpenDota, and local patch records.
- The next V3 P0 is an evidence-grounded hero item/skill/talent build-guide
  capability.
- CAP/CROO is parked and is not part of the active V3 development line.

## Archive And Deletion

- Added `docs/archive/README.md`.
- Moved these historical documents into `docs/archive/`:
  - `DotaMind_MVP_v1.md`
  - `DotaMind_MVP_v2.md`
  - `DotaMind_MVP_v2.1.md`
  - the old root `PRODUCT.md`
  - the old root `DESIGN.md`
- Deleted the stale rolling progress files that still described the 2026-06-17
  v2.1 pipeline:
  - `docs/progress/progress_zh.md`
  - `docs/progress/progress_en.md`
- Timestamped historical snapshots remain unchanged as point-in-time records.

## Technical Documentation

- Marked `docs/technical/cap-integration.md` as `parked` and changed its example
  `output_contract` to the registered `natural_language_answer` contract.
- Rewrote `docs/technical/configuration.md` against the current `.env`,
  `policy.yaml`, and `core/config.py`, including:
  - STRATZ weekly window policy.
  - `planning.sample_policy` relaxed/default/strict semantics.
  - Current environment variables and removed configuration sources.

## Boundaries

- Did not modify Python, YAML, HTML, or test code.
- Did not restore old endpoints, fallbacks, mocks, or CAP/CROO implementation.
- Did not rewrite old content or links inside timestamped historical snapshots.

## Verification

- Checked the contract registry: 4 output contracts.
- Checked the tool registry: 19 tools.
- Checked relative file links in current README, design, technical, and archive
  documentation.
- Ran `git diff --check`.
- Did not run the Python test suite for this documentation-only change.
