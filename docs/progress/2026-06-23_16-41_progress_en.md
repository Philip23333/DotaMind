# MetaMind Progress Snapshot: 2026-06-23 16:41

> This snapshot records the STRATZ transport fix from `httpx` to `urllib`.

## Background

- The local browser and `curl.exe` can access STRATZ GraphQL.
- `httpx` receives `403 text/html` from Cloudflare for the same endpoint, token, and GraphQL query.
- Both `urllib` and `requests` return `200 application/json`.

## Updates

- `apps/api/app/integrations/stratz/transport.py`
  - Replaced `httpx.AsyncClient` with standard-library `urllib.request`.
  - Preserved the async `graphql(...)` interface by running the blocking request through `asyncio.to_thread`.
  - Added `StratzHTTPStatusError` for HTTP 4xx/5xx responses.
  - Preserved GraphQL errors, non-JSON response handling, and token-safe logging.
- `apps/api/scripts/check_stratz.py`
  - Now catches `StratzHTTPStatusError`.
- `apps/api/tests/test_stratz_transport.py`
  - Reworked tests to inject a fake urllib opener.
  - Added HTTP status error coverage.
- `apps/api/tests/test_stratz_heroes.py`
  - Uses a fake transport and focuses on hero data normalization.

## Verification

- `python -m ruff check app tests scripts` passed.
- `python -m pytest tests/test_stratz_transport.py tests/test_stratz_heroes.py` passed, 7 tests.
- `python scripts/check_stratz.py --hero-id 25 --take 3` now returns live STRATZ hero matchup data.

## Conclusion

- The STRATZ token, endpoint, and GraphQL query are valid.
- The previous local failure was caused by the `httpx` request shape being blocked by STRATZ/Cloudflare.
- The STRATZ data source tool can now fetch live data through `urllib`.
