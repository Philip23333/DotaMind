# DotaMind 进度快照：2026-06-22 17:30

> 本文记录文档和配置示例同步，不包含运行时代码改动。

## 本次同步

- 根目录 `README.md` 从旧 6 Agent workflow 更新为当前 canonical pipeline：`Orchestrator -> Retriever -> Analyzer -> Critic -> Formatter`。
- `docs/technical/architecture.md` 移除了过期的 `signals.yaml`、`critic_rules.yaml` 描述，改为统一 `policy.yaml` 业务策略配置。
- `docs/technical/configuration.md` 补充旧配置源已删除的边界说明，并修正中文路径为 `apps/api/app/...`。
- `docs/technical/api.md` 增加战队报告的 `matches_in_window`、`match_details_analyzed` 字段说明，以及 `team_selection` 歧义候选选择示例。
- `apps/api/README.md` 和 `.env.example` 补齐当前 LLM、OpenDota live、Policy override、数据库和 Redis 环境变量示例。

## 当前配置边界

```text
.env                            环境、密钥、URL 和基础设施连接
apps/api/app/config/policy.yaml 唯一业务策略配置
apps/api/app/core/config.py     Settings、Policy 模型、加载和验证
apps/api/app/data/patches/      版本事实数据
apps/api/app/resources/prompts/ Prompt 内容
```

## 说明

- 本次未修改 `apps/web`，仍按协作规则视为 deprecated。
- `/debug/chat` 继续作为内部查询测试 UI。
- Policy 仍为进程级缓存，修改 YAML 或 `DOTAMIND_POLICY_PATH` 指向文件后需要重启 API。

## 验证

未运行后端测试；本次改动为 Markdown 和 `.env.example` 文档同步。
