# MetaMind Progress Snapshot: 2026-07-01 10:20

> This snapshot records planner prompt renderer hardening and `/debug/plan` model-input visibility.

## Updates

- Hardened `render_planner_tools(registry)`:
  - Each tool now renders `allowed_arg_keys`.
  - Each tool renders `produced_evidence_names_must_be_exact`.
  - References render both structured `accepted_ref` lines and readable `accepts reference from ...` lines.
  - Tool output paths render under `declared_output_paths`.
  - The renderer explicitly tells the planner to use only listed arg keys and not invent aliases or synonyms.
- Hardened `render_planner_contracts(registry)`:
  - Each output contract now renders `required_evidence_names_must_be_exact`.
  - The renderer explicitly tells the planner to copy `required_evidence` names exactly from contract/tool evidence names and not invent synonyms.
- Hardened the planner system prompt:
  - Added schema obedience rules.
  - Tool names, args keys, output contracts, and required evidence must be copied exactly from the catalogs.
  - Natural-language synonyms such as `current_roster` or `matches` must not replace real schema names.
- Added debug observability:
  - `AgenticPlannerResult` now carries `prompt_messages`.
  - `/api/v1/plan` response now includes `planner_prompt_messages`.
  - `/debug/plan` now has a `Planner Prompt Messages` details panel showing the exact system/user messages sent to the LLM.

## Verification

- Focused tests passed:
  - `python -m pytest tests/test_agentic_contracts.py tests/test_agentic_planner.py tests/test_plan_route.py tests/test_plan_console.py`
  - Result: 47 passed, 1 warning.
- Full tests passed:
  - `python -m pytest`
  - Result: 151 passed, 1 warning.
- Lint passed:
  - `python -m ruff check .`

## Boundaries

- No new agentic tool was added.
- No graph edge was changed.
- The planner prompt was not moved into `resources/prompts/*.md`; it still lives in `planner.py`.
- This change adds a debug metadata field to `/api/v1/plan`: `planner_prompt_messages`.
