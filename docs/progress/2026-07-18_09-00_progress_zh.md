# DotaMind 进度快照：2026-07-18 09:00

## 会话记忆安全修复

- 有 session 的 planner/validator 失败现在返回稳定脱敏的公开 envelope；prompt、原始模型输出、被拒绝计划和校验细节仅保留在内部，不能经 API 泄露历史。
- 内存 session 改为带 lease 的 transaction。持锁者和等待者都不能被 LRU 淘汰或替换 lock；容量可暂时超过目标值，并在 lease 释放后收敛。
- registry 契约支持强制当前计划引用。hero/team 下游工具必须引用 resolver 输出，player 下游工具必须引用成功的实时 `stratz.player_profile` 确认输出。
- `session_id` 仅接受 UUID v4。

## 验证

- 会话、planner、contract、player tool 和路由聚焦测试：`95 passed`。
- Settings 固定读取仓库根目录 `.env`；无论从 `apps/api` 还是根目录启动，均会加载已配置的 DeepSeek key。
- 完整测试：`328 passed`。
- `ruff check .` 与 `git diff --check` 通过。
