# DotaMind 进度快照：2026-06-25 17:52

> 本文记录新增 `/debug/plan` 测试页面，用于可视化 `/api/v1/plan` 的 node 流转。

## 本次更新

- 新增 `apps/api/app/resources/plan_console.html`。
- 新增 FastAPI debug 入口：
  - `GET /debug/plan`
- 页面直接调用：
  - `POST /api/v1/plan`
- 页面展示：
  - `trace` node 流转
  - `status`
  - `response_type`
  - `reason`
  - `plan`
  - `tool_results`
  - `evidence_graph`
  - `answer`
  - `review`
  - raw JSON
- 新增 `apps/api/tests/test_plan_console.py`，验证 debug 页面由 API 服务。

## 边界

- 未修改废弃前端 `apps/web`。
- 未修改 `/debug/chat`。
- 未修改 `/api/v1/query` 或旧 report pipeline。

## 验证

```bash
cd apps/api
python -m pytest tests/test_plan_console.py tests/test_query_console.py
python -m ruff check app tests scripts
```

结果：

- `2 passed`
- `ruff` 通过

