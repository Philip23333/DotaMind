# DotaMind 进度快照：2026-06-29 17:08

> 本文记录 agentic debug runner 与 contract evidence 校验 source-of-truth 修复。

## 本次更新

- 修复 `scripts/agent_plan_debug.py`：
  - `run_plan()` 现在调用 `evidence_node(state, executor.registry)`。
  - `answer_node(...)` 现在正确使用 `await`。
  - CLI 输出和退出码语义保持不变。
- 收敛 contract evidence 校验来源：
  - 删除 `contracts.py` 中手写 `EVIDENCE_KINDS`。
  - 删除静态 `validate_contract_plan(plan)` helper。
  - 保留并使用 `validate_contract_plan_with_evidence(plan, evidence_kinds)`。
  - Planner 继续从 `ToolDefinition.evidence_kinds` 汇总 known evidence kinds。
- 更新测试：
  - `test_agent_plan_debug.py` 覆盖 `run_plan()` 完整执行路径。
  - `test_agentic_contracts.py` 改为显式传入 evidence kind 集合。
  - 新增测试证明新增 registry evidence kind 不需要同步静态 allowlist。

## 边界

- 未新增 fallback。
- 未新增 mock。
- 未修改 `/api/v1/query`。
- 未修改 `/api/v1/plan` response schema。

## 验证

已运行：

```bash
cd apps/api
python -m pytest
python -m ruff check app tests scripts
```

结果：

- `161 passed, 3 skipped`
- `ruff` 通过

