# MetaMind 进度快照：2026-06-30 10:57

> 本文记录 `/api/v1/plan` 的 LangGraph runtime 迁移。

## 本次更新

- 在 `apps/api/pyproject.toml` 新增 `langgraph>=1.2.7,<2.0.0` 依赖。
- 将 `apps/api/app/agentic/graph.py` 的自定义顺序 runner 迁移为 LangGraph `StateGraph(AgentRunState)`：
  - 注册 `planner`、`validate`、`tools`、`evidence`、`answer`、`critic`、`response` 节点。
  - 用条件边复刻原有停止条件和错误边界。
  - 保留现有 node 函数中的业务逻辑，只在 runner 中做依赖注入和图编译。
  - `ainvoke()` 返回 dict 后重新校验为 `AgentRunState`，保持内部服务接口不变。
- 更新 `docs/design/MetaMind_V3_node_tool_edge_inventory.md`，标记当前 `/api/v1/plan` 已使用 LangGraph runtime。

## 边界

- 未新增 agentic tools。
- 未实现 `replan_node`。
- 未修改 `/api/v1/query` 旧链路。
- 未修改 public `PlanRequest` / `PlanResponse` schema。
- 未引入 fallback 行为。

## 验证

- `python -m pytest tests/test_agentic_graph.py tests/test_agentic_nodes.py tests/test_plan_service.py tests/test_plan_route.py`
  - 结果：22 passed。
- `python -m pytest`
  - 结果：172 passed, 3 skipped。
- `python -m ruff check .`
  - 结果：All checks passed。
