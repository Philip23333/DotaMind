# DotaMind Progress Snapshot: 2026-07-18 15:10

## Legacy frontend removal

- Deleted the complete legacy Next.js application under `apps/web`, including
  its source, configuration, fixtures, and package metadata.
- Deleted the root `package.json` and `package-lock.json`; they existed only to
  host the removed Web npm workspace and its API convenience script.
- Removed generated Web artifacts from the working copy: `apps/web/.next`, the
  TypeScript build cache, and the root `node_modules` dependency tree.
- Updated the repository README, collaboration instructions, V3 design, and
  technical architecture to define `/debug/plan` as the only internal query UI.
- Removed obsolete Node/Next.js-only ignore rules from `.gitignore`.

## Verification

- `apps/web`, root `node_modules`, root `package.json`, and root
  `package-lock.json` are absent.
- Full API suite: `333 passed, 1 warning`; the warning is FastAPI/Starlette's
  upstream `httpx` deprecation notice.
- `uv lock --check`, `ruff check .`, and `git diff --check` pass.

