# DotaMind Progress Snapshot: 2026-07-08 11:05

> This snapshot records the documentation consolidation pass: a new DotaMind
> V3.0 unified design document was added as the current primary design entry.
> No runtime code was changed.

## New Primary Design Document

- Added [DotaMind_V3.0_design.md](../design/DotaMind_V3.0_design.md).
- The document establishes **DotaMind** as the unified product name and records
  the brand-identifier debt that still existed at the time. The repository-wide
  naming pass on 2026-07-18 later resolved that debt.
- Version wording is now unified:
  - v2.5 is the constrained Tool Calling architecture foundation.
  - V3.0 is the current product capability phase, focused on the "basic complete
    capability loop."
  - V3.0 must still obey the v2.5 hard boundaries: `intent` is not a route key,
    validated `tool_calls` determine execution, and answers must be grounded in
    evidence.

## Covered Content

The new document consolidates current project state and stage goals:

- Current active API: `POST /api/v1/plan`, with `/debug/plan` as the internal
  debug UI.
- LangGraph `StateGraph(AgentRunState)` main path:
  `planner -> validate -> tools -> evidence -> answer -> critic -> response`.
- Implementation map for `application`, `agentic/planning`, `agentic/tools`,
  `agentic/evidence`, `agentic/answer`, `agentic/critic`, `agentic/nodes`,
  `integrations`, and `config/policy.yaml`.
- Core data models: `ExecutionPlan`, `ToolCall`, `QueryContext`, `ToolResult`,
  `EvidenceGraph`, and `AgentRunState`.
- Current 19-tool registry and evidence kinds:
  - STRATZ / player / lane / matchup / synergy / position / daily-trend tools.
  - OpenDota team / role-meta tools.
  - Local patch tools.
- Current 4 output contracts: `natural_language_answer`,
  `patch_impact_report`, `role_meta_report`, and `team_recent_report`.
- STRATZ data constraints, completed-week / day-grain boundaries, and
  bracket/rank filter boundaries.
- Sample Policy: explicit / strict / relaxed / default modes and
  `policy_applied` provenance.
- Closed V3.0 capabilities, P0/P1 gaps, non-goals, test acceptance, and naming
  migration strategy.

## Wording Correction

- Calling the current state only "v2.5" is not precise enough anymore.
- The review stance should now be:
  - **V3.0 is the current product goal.**
  - **v2.5 is the architectural constraint foundation V3.0 must obey.**
- Some docs, prompts, and docstrings still used inconsistent brand wording at
  the time. The repository-wide naming pass on 2026-07-18 later resolved it.

## Verification

- Documentation-only change; the test suite was not run.
- Read-only code introspection confirmed the current registry has 19 tools and
  the contract registry has 4 output contracts.

## Suggested Next Steps

1. Gradually update README, `docs/technical/architecture.md`, planner prompt,
   and `/debug/plan` copy to the DotaMind V3.0 wording.
2. Maintain a dedicated V3.0 capability-loop acceptance matrix.
3. Start G1 hero build-guide schema exploration, or proceed with Prompt Registry
   / bounded critic->planner retry work.

