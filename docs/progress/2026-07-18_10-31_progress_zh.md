# DotaMind 进度快照：2026-07-18 10:31

## 全仓命名统一

- 产品展示名统一为 `DotaMind`，覆盖 API、Planner/Answer 文案、调试页面、
  User-Agent、Web UI、脚本错误信息、README、设计文档、技术文档、归档和历史
  进度记录。
- 环境变量统一使用 `DOTAMIND_*` 前缀；`Settings`、`.env.example`、本地
  `.env`、STRATZ 脚本、错误信息和文档已同步。
- Python distribution 统一为 `dotamind-api`，并同步 `pyproject.toml`、
  `uv.lock` 和本地虚拟环境生成元数据。
- npm workspace 统一为 `dotamind`，Web package 统一为 `@dotamind/web`，
  `package.json` 与 `package-lock.json` 已同步。
- PostgreSQL 默认数据库、用户、密码和默认连接 URL 统一为 `dotamind`。
- `LICENSE`、Docker Compose、启动脚本和所有测试断言已同步。

## 文档文件名

当前设计和归档文件统一使用 DotaMind 文件名：

- `docs/design/DotaMind_MVP_v2.5.md`
- `docs/design/DotaMind_V3_node_tool_edge_inventory.md`
- `docs/archive/DotaMind_MVP_v1.md`
- `docs/archive/DotaMind_MVP_v2.md`
- `docs/archive/DotaMind_MVP_v2.1.md`

所有活动文档链接、`AGENTS.md` 和历史文本引用已同步。V3 主设计中的命名迁移
章节已改为当前命名规范和明确的兼容性边界。

## 兼容性边界

- 本项目仍处于开发期，不保留此前品牌标识的环境变量、包名或文档路径兼容层。
- 部署环境和本地开发环境必须使用 `DOTAMIND_*`。
- 已有 Docker PostgreSQL volume 若只初始化了不同的旧数据库/用户，需要按新的
  `dotamind` 默认值重新初始化或手工迁移。

## 验证

- 全仓旧品牌内容残留：`0`。
- 工作树旧品牌文件名残留：`0`。
- 本地 `.env` 和虚拟环境旧品牌生成元数据残留：`0`。
- 当前活动文档相对链接断链：`0`。
- `uv lock --check` 通过。
- `npm ls --depth=0` 与 package JSON 解析通过。
- API 完整测试：`333 passed, 1 warning`；warning 为 FastAPI/Starlette 上游
  `httpx` 弃用提示。
- `ruff check .` 通过。
- Web `npm run typecheck --workspace apps/web` 通过。
- `git diff --check` 通过，仅有 Git 的 LF/CRLF 工作树提示。
