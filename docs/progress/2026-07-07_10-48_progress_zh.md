# MetaMind 进度快照：2026-07-07 10:48

> 本文记录 P2 Commit 1（一致性 + planner prompt 修正）：selection_policy 跨 ranking 工具统一、region/gameMode 不静默校验、position 透传明示 + 别名表（修正中单污染）、lane_meta 描述修正。

## 修改总结

- **selection_policy 统一**（[stratz_tools.py](apps/api/app/agentic/tools/stratz_tools.py)）：`hero_matchup_ranking` / `hero_synergy_ranking` 的 handler 返回值加 `selection_policy` 字符串（与 `lane_meta_global` / `hero_position_stats` 对齐），让 4 个 ranking 工具的排序口径都到达 answer。
- **region/gameMode 不静默**（[contracts.py](apps/api/app/agentic/planning/contracts.py) `validate_context_scope`）：`context.region_ids`/`game_mode_ids` 若与非 `hero_daily_trends` 工具组合，校验报错（不静默忽略）。planner prompt 同步说明 schema 限制。
- **position 别名表 + 修正**（[planner.py](apps/api/app/agentic/planning/planner.py)）：POSITION_1=carry/大哥、**POSITION_2=mid/中单**（修正之前「中单」污染 1 号位的硬伤）；「support」单独出现（无 4/5/soft/hard 限定）不默认映射 POSITION_4，要求澄清。
- **position 透传明示**：planner + `lane_meta_global` description 明示 lane_meta_global 忽略 position（全局视角，刻意丢弃 laneOutcome positionIds），指定英雄组合用 `pair_lane_outcome`。
- **测试**（[test_agentic_contracts.py](apps/api/tests/test_agentic_contracts.py)）：region + 非 daily 报错 / region + daily_only 放行。

## 关键设计

- **unsupported scope 硬保障在 validate 层**（不依赖 LLM 自觉）：region/gameMode + 非 daily 工具 → 校验失败 → planner 重试或 insufficient_tools。比仅靠 prompt 更可靠。
- **别名表修正**：中单 = POSITION_2（mid），不是 POSITION_1（carry）；「support」模糊词要求澄清，避免默认 4 号位。

## 验证

- 全套 pytest（含 contracts/registry/planner/critic/evidence）→ 116 passed。
- 新增 region 校验测试 2 个（拒绝 region+matchup / 放行 region+daily）。

## 边界

- P2 Commit 2（filter_heroes_by_position）/ Commit 3（扇出 helper）未做。
- region/gameMode 仍只在 hero_daily_trends schema 支持，其他工具不传（校验守住不静默）。
