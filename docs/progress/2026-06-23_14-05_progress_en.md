# MetaMind Progress Snapshot: 2026-06-23 14:05

> This document records Critic quality-gate step 3: moving quality thresholds into the unified `policy.yaml`.

## Updates

- Added Critic quality-gate configuration under `apps/api/app/config/policy.yaml`:
  - `mock_allowed`
  - `min_confidence`
  - `hard_min_confidence`
  - `team_report.max_latest_match_age_days`
  - `team_report.hard_max_latest_match_age_days`
  - `team_report.min_matches_in_window`
  - `team_report.min_match_details_analyzed`
- Added Pydantic models and validation for Critic quality gates in `apps/api/app/core/config.py`.
- `CriticAgent` now reads thresholds from `get_policy().critic` instead of internal constants.
- Updated `docs/technical/configuration.md` to state that Critic policy now covers mock, confidence, freshness, and sample-size quality gates.

## Validation Rules

- `hard_min_confidence` cannot exceed `min_confidence`.
- `team_report.max_latest_match_age_days` cannot exceed `team_report.hard_max_latest_match_age_days`.
- All Critic numeric thresholds have range constraints.
- Policy still rejects unknown fields.

## Current Boundary

- API behavior is unchanged: Critic warning/failed status is exposed only through the `tasks` trace and does not change the HTTP status.
- A later step can map `quality gate failed` to `422` or a structured `quality_failure` response at the route/service layer.

## Verification

```text
python -m pytest
55 passed, 3 skipped, 1 warning

python -m ruff check app tests
All checks passed!
```

