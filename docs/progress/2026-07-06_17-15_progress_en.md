# DotaMind Progress Snapshot: 2026-07-06 17:15

> This snapshot records the STRATZ tools P0 correctness convergence (audit doc §4 P0-1/P0-2/P0-3). Goal: before any P1 capability work, converge the three correctness issues — win-rate caliber, sort ownership, and sort key. Sources: `docs/design/STRATZ工具审计与重构输入.md` §4 + plan `enchanted-humming-floyd.md`.

## Modification Summary

- **P0-2 sort ownership (Commit 1)**: removed the in-place `sort` from `_normalize_matchup_side`; the integration layer is again a thin relay (normalizes field names only) and preserves STRATZ's raw iteration order. Sorting/top-K stays in the agentic layer `_filter_matchup_rows` (synergy desc + match_count tie-break, already there).
- **P0-3 dead-field removal + de-over-fetch (Commit 1)**:
  - Dropped `target_win_rate` / `hero_win_rate` (= provider native `winRateHeroId1/2`), which never reached evidence.
  - Removed `winRateHeroId1` / `winRateHeroId2` from both `vs {}` blocks of `_HERO_VS_HERO_MATCHUP_QUERY` to stop over-fetching.
  - Sort key fixed to `synergy` (STRATZ's composite matchup advantage score, sample-weighted); no `selection_mode` (matchup already has advantage/disadvantage natural grouping).
- **P0-1 explicit win-rate caliber naming (Commit 2)**:
  - Renamed matchup's local `win_rate` → `matchup_win_rate` (explicit "target hero's game win rate vs that opponent = winCount/matchCount"), distinguishing it from lane's `match_win_rate` (match-level).
  - Added a `win_rate_basis` tag to all three win-rate evidences (pair_lane_winrate / lane_meta_row / matchup_ranking_row) in `value`, **mirrored at top-level and in `filters`**, so the answer LLM (which sees only the evidence graph) can phrase caliber correctly.
  - Updated `hero_matchup_ranking` and `lane_meta_global` descriptions with caliber notes (planner-visible contract).

## Key Findings / Corrections

- Audit doc §2.2 originally said "local `win_rate` is basically a dead field" — **inaccurate**: verification shows `win_rate` does reach evidence (`hero_matchup_ranking_evidence` propagates it), so it is not dead, merely too generically named; the truly never-propagated dead fields are `target_win_rate` / `hero_win_rate`. Audit doc §2.2 and §4 P0-3 wording corrected accordingly.
- The `win_rate_basis` placement is fixed as "top-level primary + filters mirror": top-level describes the numeric field's caliber; the filters mirror honors the provenance-in-filters convention (memory `answer-sees-evidence-graph-only`).

## Verification

- `pytest tests/test_stratz_heroes.py tests/test_agentic_stratz_tools.py tests/test_agentic_evidence.py tests/test_agentic_graph.py tests/test_agentic_answer.py tests/test_plan_service.py tests/test_agentic_critic.py` → 50 passed.
- Added `test_stratz_heroes_matchup_preserves_stratz_order`: two advantage rows deliberately in reverse synergy order; asserts the integration layer preserves STRATZ raw order (guarding the "sort happens in the agentic layer" contract).
- `FakeTransport` now records `last_query`; asserts the GraphQL query text does not contain `winRateHeroId1/2` (de-over-fetch is test-covered).
- `test_agentic_evidence.py` gained top-level + filters `win_rate_basis` assertions.
- Acceptance grep (scoped to stratz):
  - `target_win_rate|hero_win_rate` in `apps/api/app` → none.
  - `winRateHeroId` in `heroes.py` → none.
  - `"win_rate"` in `apps/api/app/integrations/stratz` + `stratz_tools.py` → none (only `match_win_rate` / `matchup_win_rate` remain).

## Boundaries

- P1 not done: winDay trend tool, `hero_position_stats` `winCount` extension, laneOutcome stomp/cs fields, matchup `with` sub-query.
- P2 not done: per-week fan-out helper, `region_ids/game_mode_ids` alignment, position-filter pass-through, `matchLimit` server-side filtering, cross-tool selection_policy unification.
- All do-not-break constraints preserved: `match_win_rate = matchWinCount/matchCount` (match-level) unchanged; week-window logic untouched; thin relay did not invent aggregate scores.
