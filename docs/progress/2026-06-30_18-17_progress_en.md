# DotaMind Progress Snapshot: 2026-06-30 18:17

> This snapshot records Tool Contract Runtime consistency hardening.

## Updates

- Fixed AcceptedRef type consistency risk:
  - Added `validate_registry_contracts(registry)`.
  - Validates that `arg_contracts` reference real input fields.
  - Validates that `AcceptedRef.type` is compatible with the target field's `input_model` annotation.
  - Validates that `AcceptedRef.from_tool/path/type` matches the source tool's declared `output_paths`.
- Removed the `ArgContract.required` drift point:
  - Required/optional rendering now comes only from Pydantic `input_model.model_fields[name].is_required()`.
  - `arg_contracts` now only express field semantics and accepted references.
- Added tests:
  - Dummy contract target-field type incompatibility.
  - Dummy contract unknown arg contract.
  - Dummy accepted ref type mismatch with source output path type.

## Docs

- Updated `docs/design/DotaMind_MVP_v2.5.md` with the current limitation:
  - validator can detect `$...` references inside lists/dicts.
  - `_replace_references()` is not yet fully element-type-aware for nested `list[T]` / `dict[K,V]`.
  - If list-internal references are allowed later, recursive annotation propagation is required.
- Mirrored the limitation in `docs/technical/architecture.md`.

## Verification

- Focused tests passed:
  - `python -m pytest tests/test_agentic_contracts.py tests/test_agentic_planner.py tests/test_agentic_nodes.py tests/test_agentic_graph.py tests/test_plan_service.py`
  - Result: 65 passed.
- Full tests passed:
  - `python -m pytest`
  - Result: 151 passed, 1 warning.
- Lint passed:
  - `python -m ruff check .`

## Boundaries

- Did not implement full type-aware placeholders for nested list/dict references.
- Did not add agentic tools.
- Did not change `/api/v1/plan` public response fields.
