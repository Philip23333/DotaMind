# MetaMind Progress Snapshot: 2026-06-30 16:58

> This snapshot records the project-level documentation constraint for `intent` semantics in the agentic planner.

## Updates

- Updated `AGENTS.md`:
  - Corrected the internal test UI from deleted `/debug/chat` to `/debug/plan`.
  - Added `Agentic Planning Semantics`, defining `intent` as a semantic label for the user's goal, not a routing key.
- Updated `docs/design/MetaMind_MVP_v2.5.md`:
  - Added an `Intent 语义边界` section.
  - Clarified that execution is determined by `tool_calls`, response shape by `output_contract`, and evidence obligations by `required_evidence` plus contract rules.
  - Used the `lane_outcome` example to show that the planner should infer required args from tool schema, not from an intent-specific pipeline or example.
  - Recorded that the current `draft_advice` / `counter_pick` binding is only a temporary guardrail.
- Updated `docs/design/MetaMind_V3_node_tool_edge_inventory.md`:
  - Added intent semantics to the current implementation shape to prevent future regressions toward old `task_type` routing.

## Boundaries

- Documentation-only update.
- Did not modify API code.
- Did not add tools, contracts, validator behavior, or graph edges.
- Did not modify `apps/web`.

## Verification

- Manually reviewed the documentation updates.
- Used text search to confirm the key phrases are present in the project-level and design docs.

