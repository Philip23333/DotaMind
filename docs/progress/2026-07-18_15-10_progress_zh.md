# DotaMind 进度快照：2026-07-18 15:10

## 遗留前端删除

- 完整删除 `apps/web` 下的旧 Next.js 应用，包括源码、配置、fixture 与包元数据。
- 删除根目录 `package.json` 和 `package-lock.json`；它们仅用于承载已删除的
  Web npm workspace 及其 API 快捷脚本。
- 清理工作区中的 Web 生成物：`apps/web/.next`、TypeScript 构建缓存和根目录
  `node_modules` 依赖树。
- 更新仓库 README、协作说明、V3 设计和技术架构，明确 `/debug/plan` 是唯一的
  内部查询测试 UI。
- 从 `.gitignore` 删除仅服务于 Node/Next.js 的过时规则。

## 验证

- `apps/web`、根目录 `node_modules`、`package.json` 和 `package-lock.json`
  均已不存在。
- API 完整测试：`333 passed, 1 warning`；warning 为 FastAPI/Starlette 上游
  `httpx` 弃用提示。
- `uv lock --check`、`ruff check .` 与 `git diff --check` 通过。

