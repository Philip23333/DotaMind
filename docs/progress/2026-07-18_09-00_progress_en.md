# DotaMind Progress Snapshot: 2026-07-18 09:00

## Session-memory safety repair

- Stateful planner and validation failures now return a stable redacted public
  envelope; planner prompts, raw output, rejected plans, and validation details
  remain internal and cannot expose stored history through the API.
- In-memory sessions now use lease-aware transactions. Active holders and
  waiters cannot be evicted or have their lock replaced; capacity may exceed the
  LRU target temporarily and converges after leases release.
- Registry contracts can require a current-plan reference. Hero/team data tools
  require resolver output, and player data tools require a successful live
  `stratz.player_profile` confirmation.
- `session_id` accepts UUID v4 only.

## Verification

- Focused session, planner, contract, player-tool, and route tests: `95 passed`.
- Settings load the repository-root `.env`, including the configured DeepSeek
  key, regardless of whether the API starts from `apps/api` or the root.
- Full suite: `328 passed`.
- `ruff check .` and `git diff --check` pass.
