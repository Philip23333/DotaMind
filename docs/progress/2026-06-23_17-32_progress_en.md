# DotaMind Progress Snapshot: 2026-06-23 17:32

> This document records Chinese common-alias expansion for the local Dota 2 hero mapping table.

## Updates

- Expanded the `aliases` fields in `apps/api/app/data/heroes/dota2_heroes.yaml`.
- Added common Chinese internet names, legacy Dota Chinese names, community shorthand, and selected English abbreviations, such as `剑圣`, `屠夫`, `小鱼人`, `老奶奶`, `白虎`, `水人`, `卡尔`, and `黑鸟`.
- Preserved the existing single-line YAML array style and did not change hero `id`, OpenDota `name`, or `localized_name` values.
- Updated `apps/api/tests/test_agentic_hero_resolver.py` with default-resolver coverage for newly added Chinese aliases.

## Notes

- This change only updates local hero resolution data and its focused tests; OpenDota, STRATZ, and the report pipeline were not changed.
- `es` remains an intentionally ambiguous alias for `Earthshaker` and `Earth Spirit`; the existing test continues to cover that behavior.
- Overly short or generic Chinese aliases were avoided to reduce false-positive resolution.

## Verification

- `python -c "from app.agentic.hero_resolver import load_hero_constants, HERO_CONSTANTS_PATH; heroes=load_hero_constants(HERO_CONSTANTS_PATH); print(len(heroes)); print(sum(len(h.aliases) for h in heroes))"`
- `python -m pytest tests/test_agentic_hero_resolver.py -q`
