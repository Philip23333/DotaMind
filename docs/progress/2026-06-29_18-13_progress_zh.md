# MetaMind 进度快照：2026-06-29 18:13

> 本文记录 Planner Contract `allowed_evidence` 渲染修复。

## 本次更新

- 修复 `render_planner_contracts()` 对无限制 contract 的误导性输出。
- 当 contract 没有 `allowed_evidence` / `allowed_required_evidence` 时，现在渲染为：
  - `allowed_evidence: null`
- 有 allowlist 的 contract 仍渲染 allowlist 与 registry evidence 的交集。
- validation 语义不变：
  - `allowlist is None` 表示不做 contract-specific allowed evidence 限制。
  - required evidence 仍必须来自 registry 的 `evidence_kinds`。

## 边界

- 未修改 Planner prompt 其它内容。
- 未修改 `/api/v1/query`。
- 未新增 fallback。
- 未新增 mock。

## 验证

已运行：

```bash
cd apps/api
python -m pytest tests/test_agentic_contracts.py tests/test_agentic_planner.py
python -m ruff check app tests scripts
python -m pytest
```

结果：

- 目标测试：`32 passed`
- 全量测试：`172 passed, 3 skipped`
- `ruff` 通过
