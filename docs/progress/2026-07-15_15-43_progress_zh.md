# DotaMind 进度快照：2026-07-15 15:43

> 本文记录 `feature/v3-functional-loop` 的文档结构收敛。本轮只修改文档，
> 未修改运行时代码。

## 文档入口

- 新增 `docs/README.md`，定义当前文档阅读顺序和权威等级：
  - `DotaMind_V3.0_design.md` 是当前产品与能力主设计。
  - `DotaMind_MVP_v2.5.md` 是 V3 必须遵守的架构底座。
  - `technical/architecture.md` 是当前代码与运行时地图。
- 根 `README.md` 已更新为 DotaMind V3 口径，并链接新的文档入口。
- `apps/api/README.md` 已与当前 API、contract registry 和 tool registry 对齐。

## 当前代码口径

只读核对当前工作树后，文档统一为：

- active endpoints：`GET /health`、`POST /api/v1/plan`、`GET /debug/plan`。
- output contracts 共 4 个：
  - `natural_language_answer`
  - `patch_impact_report`
  - `role_meta_report`
  - `team_recent_report`
- agentic registry 共 19 个工具，覆盖本地英雄常量、STRATZ、OpenDota 和本地
  patch records。
- 当前 V3 下一 P0 是英雄物品、技能和天赋 build guide 证据链。
- CAP/CROO 暂停，不属于当前 V3 开发主线。

## 归档与删除

- 新增 `docs/archive/README.md`。
- 将以下历史文档移入 `docs/archive/`：
  - `DotaMind_MVP_v1.md`
  - `DotaMind_MVP_v2.md`
  - `DotaMind_MVP_v2.1.md`
  - 根目录旧 `PRODUCT.md`
  - 根目录旧 `DESIGN.md`
- 删除 2026-06-17 后已失真的滚动进度文件：
  - `docs/progress/progress_zh.md`
  - `docs/progress/progress_en.md`
- 历史时间戳快照保持不变，仍作为当时状态记录。

## 技术文档

- `docs/technical/cap-integration.md` 已明确标记为 `parked`，并将示例
  `output_contract` 改为当前 registry 中存在的 `natural_language_answer`。
- `docs/technical/configuration.md` 已按当前 `.env`、`policy.yaml` 和
  `core/config.py` 重写，补充：
  - `stratz` weekly window policy。
  - `planning.sample_policy` 的 relaxed/default/strict 阈值语义。
  - 当前环境变量与已删除配置源。

## 边界

- 未修改 Python、YAML、HTML 或测试代码。
- 未恢复旧 endpoint、fallback、mock 或 CAP/CROO 实现。
- 未重写历史时间戳进度快照中的旧内容或旧链接。

## 验证

- 核对 contract registry：4 个 output contracts。
- 核对 tool registry：19 个工具。
- 检查当前 README、design、technical、archive 文档的相对文件链接。
- 运行 `git diff --check`。
- 文档变更未运行 Python 测试套件。
