# DotaMind 进度快照：2026-06-29 17:20

> 本文记录 `/debug/plan` 调试页面输出分层与 Planner debug payload 更新。

## 本次更新

- `AgenticPlannerResult` 新增 `raw_output`，保存 LLM 成功解析后的 planner JSON envelope。
- `/api/v1/plan` response 新增 `planner_output` 字段。
- `response_node` 会把 `planning.raw_output` 写入 `planner_output`。
- `/debug/plan` 页面新增三块可读输出：
  - `Planner LLM JSON`
  - `Final User Output`
  - `Parsed Final Data`
- 页面仍保留完整 `Raw JSON`，用于底层排查。
- `Parsed Response` 标题改为 `Readable Output`，区分用户可读输出和完整原始响应。

## 边界

- 未修复 Planner JSON 截断问题。
- Planner JSON 解析失败时仍只能显示错误，因为当前 provider 不暴露半截原始 content。
- 未新增 fallback。
- 未新增 mock。
- 未修改 `/api/v1/query`。

## 验证

已运行：

```bash
cd apps/api
python -m pytest
python -m ruff check app tests scripts
```

结果：

- `161 passed, 3 skipped`
- `ruff` 通过

