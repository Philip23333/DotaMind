# DotaMind Progress Snapshot: 2026-06-29 17:08

> This snapshot records fixes for the agentic debug runner and contract evidence validation source of truth.

## Updates

- Fixed `scripts/agent_plan_debug.py`:
  - `run_plan()` now calls `evidence_node(state, executor.registry)`.
  - `answer_node(...)` is now correctly awaited.
  - CLI output and exit-code semantics are unchanged.
- Consolidated contract evidence validation:
  - Removed the handwritten `EVIDENCE_KINDS` set from `contracts.py`.
  - Removed the static `validate_contract_plan(plan)` helper.
  - Kept `validate_contract_plan_with_evidence(plan, evidence_kinds)`.
  - Planner continues to derive known evidence kinds from `ToolDefinition.evidence_kinds`.
- Updated tests:
  - `test_agent_plan_debug.py` now covers the full `run_plan()` path.
  - `test_agentic_contracts.py` now passes evidence kinds explicitly.
  - Added coverage proving new registry-declared evidence kinds do not require a static allowlist update.

## Boundaries

- Did not add fallback behavior.
- Did not add mock data.
- Did not change `/api/v1/query`.
- Did not change the `/api/v1/plan` response schema.

## Verification

Ran:

```bash
cd apps/api
python -m pytest
python -m ruff check app tests scripts
```

Result:

- `161 passed, 3 skipped`
- `ruff` passed

