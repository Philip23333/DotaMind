# DotaMind 进度快照：2026-06-30 16:58

> 本文记录 agentic planner 中 `intent` 语义边界的项目级文档约束。

## 本次更新

- 更新 `AGENTS.md`：
  - 将内部测试 UI 从已删除的 `/debug/chat` 修正为 `/debug/plan`。
  - 新增 `Agentic Planning Semantics`，明确 `intent` 是用户目标语义标签，不是路由键。
- 更新 `docs/design/DotaMind_MVP_v2.5.md`：
  - 新增 `Intent 语义边界` 小节。
  - 明确执行由 `tool_calls` 决定，响应形态由 `output_contract` 决定，证据义务由 `required_evidence` 和 contract rules 决定。
  - 用 `lane_outcome` 示例说明 planner 应从 tool schema 推导必填参数，而不是依赖 intent 专属 pipeline 或 example。
  - 记录当前 `draft_advice` 与 `counter_pick` 的绑定只能视为临时 guardrail。
- 更新 `docs/design/DotaMind_V3_node_tool_edge_inventory.md`：
  - 在当前实现形态中补充 intent semantics，防止后续把 `intent` 重新做成旧 `task_type`。

## 边界

- 本次只更新文档。
- 未修改 API 代码。
- 未新增 tools、contracts、validator 行为或 graph edge。
- 未修改 `apps/web`。

## 验证

- 手动检查文档更新。
- 使用文本搜索确认关键短语已写入项目级和设计文档。

