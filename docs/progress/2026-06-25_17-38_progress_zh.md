# MetaMind 进度快照：2026-06-25 17:38

> 本文记录 V3 agentic tools 与 response node 的落地。本次继续推进 `/api/v1/plan`，不接旧 pipeline，不 fallback，不新增 mock。

## 本次更新

- 新增 `apps/api/app/agentic/opendota_tools.py`，把已有 OpenDota integration 拆成可注册 agentic tools：
  - `opendota.resolve_team`
  - `opendota.team_recent_matches`
  - `opendota.team_players`
  - `opendota.team_heroes`
  - `opendota.hero_stats_by_role`
- 扩展 `apps/api/app/agentic/stratz_tools.py`：
  - 新增 `stratz.lane_outcome`
  - 默认 registry 现在注册 Local hero resolver、STRATZ tools、OpenDota tools。
- 扩展 `EvidenceGraph` 聚合：
  - `team_identity`
  - `recent_matches`
  - `current_players`
  - `team_hero_usage`
  - `match_detail_sample`
  - `hero_stats`
  - `role_fit`
  - `lane_outcome`
  - `sample_size`
- 将 `formatter_node` 替换为 `response_node`：
  - `AgentRunState` 和 `/api/v1/plan` response 新增 `response_type`
  - 当前 response types：
    - `capability_boundary`
    - `execution_error`
    - `draft_advice`
    - `unsupported_answer`
    - `raw_tool_results`
- 更新 `AgenticPlanner`：
  - system prompt 的 allowed tools 来自 registry。
  - `stratz.hero_vs_hero_matchup` 和 `stratz.lane_outcome` 都禁止硬编码 `hero_id`，必须引用 `"$resolve_target.data.hero.hero_id"`。

## 行为边界

- OpenDota 工具只返回结构化证据，不生成 team report。
- team/meta evidence plan 可以执行到 EvidenceGraph，但 AnswerSynthesizer 不支持时会显式返回 `unsupported_output_contract`。
- OpenDota/STRATZ 错误通过 `ToolResult.status="error"` 暴露，不降级 mock。
- `/api/v1/query`、`/debug/chat`、旧 report pipeline 未修改。

## 验证

```bash
cd apps/api
python -m pytest
python -m ruff check app tests scripts
```

结果：

- `131 passed, 3 skipped`
- `ruff` 通过

