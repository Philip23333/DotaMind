# MetaMind Progress Snapshot: 2026-06-22 17:30

> This document records documentation and configuration-example synchronization. No runtime code was changed.

## Updates

- Updated root `README.md` from the old six-Agent workflow to the current canonical pipeline: `Orchestrator -> Retriever -> Analyzer -> Critic -> Formatter`.
- Removed stale `signals.yaml` and `critic_rules.yaml` references from `docs/technical/architecture.md`; the document now points to unified `policy.yaml` business-policy configuration.
- Updated `docs/technical/configuration.md` with the removed legacy configuration boundary and corrected Chinese paths to `apps/api/app/...`.
- Added `matches_in_window`, `match_details_analyzed`, and `team_selection` ambiguous-candidate selection details to `docs/technical/api.md`.
- Expanded current LLM, OpenDota live, Policy override, database, and Redis environment-variable examples in `apps/api/README.md` and `.env.example`.

## Current Configuration Boundary

```text
.env                            environment, secrets, URLs, infrastructure connections
apps/api/app/config/policy.yaml single business-policy source
apps/api/app/core/config.py     Settings, Policy models, loading, validation
apps/api/app/data/patches/      patch fact data
apps/api/app/resources/prompts/ prompt content
```

## Notes

- `apps/web` was not modified and remains deprecated by the collaboration rules.
- `/debug/chat` remains the internal query test UI.
- Policy remains cached for the process lifetime; restart the API after editing YAML or a file pointed to by `METAMIND_POLICY_PATH`.

## Verification

Backend tests were not run; this change only synchronizes Markdown documentation and `.env.example`.
