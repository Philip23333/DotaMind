# MetaMind Progress Snapshot: 2026-07-07 10:48

> This snapshot records P2 Commit 1 (consistency + planner prompt fixes): selection_policy unified across ranking tools, region/gameMode no-silent-ignore validation, position pass-through clarity + alias table (fixing the mid-lane alias bug), lane_meta description fix.

## Modification Summary

- **selection_policy unified** ([stratz_tools.py](apps/api/app/agentic/tools/stratz_tools.py)): `hero_matchup_ranking` / `hero_synergy_ranking` handler returns add a `selection_policy` string (aligned with `lane_meta_global` / `hero_position_stats`), so all 4 ranking tools carry their sort basis to the answer layer.
- **region/gameMode no-silent-ignore** ([contracts.py](apps/api/app/agentic/planning/contracts.py) `validate_context_scope`): if `context.region_ids`/`game_mode_ids` are combined with any non-`hero_daily_trends` tool, validation raises an error (not silently ignored). Planner prompt states the schema limit too.
- **position alias table + fix** ([planner.py](apps/api/app/agentic/planning/planner.py)): POSITION_1=carry/大哥, **POSITION_2=mid/中单** (fixes the earlier bug where "中单" polluted position 1); "support" alone (without 4/5/soft/hard qualifier) is not defaulted to POSITION_4 — the planner must ask for clarification.
- **position pass-through clarity**: planner + `lane_meta_global` description state lane_meta_global ignores position (global view, laneOutcome positionIds dropped by design); for a specific hero+pair use `pair_lane_outcome`.
- **tests** ([test_agentic_contracts.py](apps/api/tests/test_agentic_contracts.py)): region + non-daily rejected / region + daily_only allowed.

## Key Design

- **unsupported scope is enforced at the validate layer** (not relying on LLM self-discipline): region/gameMode + non-daily tools -> validation failure -> planner retry or insufficient_tools. More reliable than prompt alone.
- **alias fix**: mid = POSITION_2 (not POSITION_1=carry); "support" is ambiguous and must be clarified, never defaulted to POSITION_4.

## Verification

- Full pytest suite (incl. contracts/registry/planner/critic/evidence) -> 116 passed.
- Added 2 region-validation tests (reject region+matchup / allow region+daily).

## Boundaries

- P2 Commit 2 (filter_heroes_by_position) / Commit 3 (fan-out helper) not done.
- region/gameMode still only supported by hero_daily_trends schema; other tools do not pass them (validation enforces no silent ignore).
