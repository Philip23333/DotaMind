# DotaMind Progress Snapshot: 2026-07-03 15:43

> This snapshot records the "v2.5 layered workflow documentation" update. Scope is design documentation only: the current agentic backend is split into Planner, Validator, Tool, Evidence, and Answer + Critic layers, with each layer documenting workflow, inputs/outputs, failure paths, boundaries, and extension rules.

## Modification Summary

- **Goal**: turn the previous architecture discussion into durable design documents, so later work on validators, STRATZ time windows, tool expansion, and answer/critic boundaries can refer to precise layer responsibilities.
- **Added docs** (5 files under `docs/design/`):
  - `Planner层.md`: prompt construction, LLM envelope, Pydantic validation, catalog-validation retry, and graph routing from user query to `ExecutionPlan`.
  - `Validator层.md`: Pydantic vs catalog validation, plus the registry/tool/ref/args/output_contract/evidence/context-scope validation chain.
  - `Tool层.md`: `ToolDefinition`, `ToolRegistry`, runtime `$ref` resolution, `ToolExecutor`, `QueryContext`, and `ToolResult` execution flow.
  - `Evidence层.md`: `ToolResult -> EvidenceItem -> EvidenceGraph`, missing evidence, data quality, STRATZ weekly bucket provenance, and extractor boundaries.
  - `Answer+Critic层.md`: structured/natural answer routing, evidence-only natural-language prompt, data notes/confidence, critic checks, and response serialization.

## Alignment Points

- The docs follow `docs/design/DotaMind_MVP_v2.5.md` and `docs/technical/architecture.md`: `intent` is not a routing key, and execution is determined only by validated `tool_calls`.
- The docs describe STRATZ time windows according to the current `weeks_back` working-tree semantics: Planner expresses relative completed-week count, Tool resolves concrete weeks, and Evidence/Answer preserve per-week provenance.
- The docs explicitly state that Validator does not perform business routing, execute tools, or silently clamp values; Tool does not produce conclusions; Evidence does not aggregate cross-week statistics; Answer/Critic do not add tools.

## Verification

- Confirmed the five new layer docs exist and scanned their heading structure.
- Did not run pytest because this turn only adds documentation.

## Boundaries

- No implementation code was modified.
- Existing `weeks_back` working-tree changes were not touched.
- `DotaMind_MVP_v2.5.md` was not edited; this turn adds companion layer documents.

