# MetaMind 进度快照：2026-06-30 14:48

> 本文记录 `apps/api/app/agentic` 的目录结构重组。

## 本次更新

- 将 `agentic/` 从平铺模块重组为职责分包：
  - `nodes/`：planner、validate、tools、evidence、answer、critic、response 节点。
  - `tools/`：registry、executor、hero、patch、OpenDota、STRATZ、ranking tool 入口。
  - `planning/`：planner 与 output contract catalog。
  - `evidence/`：EvidenceGraph 和 extraction helper。
  - `answer/`：answer synthesizer、structured/natural 入口。
  - `critic/`：reviewer 和 critic rules 入口。
- 保留 `graph.py`、`state.py`、`models.py` 在 `agentic/` 顶层，作为 LangGraph runner、共享状态和公共模型。
- 拆分 `ToolExecutor` 到 `agentic/tools/executor.py`，`ToolRegistry`/`ToolDefinition` 保留在 `agentic/tools/registry.py`。
- 将 hero resolver 移入 `agentic/tools/hero_tools.py`，并修正英雄常量路径。
- 更新代码、脚本、测试 import 到新结构。
- 更新 architecture 和 V3 inventory 文档中的 agentic 目录说明。

## 边界

- 未修改 `/api/v1/plan` public response shape。
- 未新增 agentic tools 或 output contracts。
- 未恢复旧 report/query 链路。
- 未修改 `apps/web`。

## 验证

- `python -m pytest tests/test_agentic_graph.py tests/test_agentic_nodes.py tests/test_plan_service.py tests/test_plan_route.py tests/test_plan_console.py`
  - 结果：24 passed。
- `python -m pytest`
  - 结果：139 passed。
- `python -m ruff check .`
  - 结果：All checks passed。

