# DotaMind Progress Snapshot: 2026-06-30 14:48

> This snapshot records the `apps/api/app/agentic` directory restructuring.

## Updates

- Reorganized `agentic/` from flat modules into responsibility-based packages:
  - `nodes/`: planner, validate, tools, evidence, answer, critic, and response nodes.
  - `tools/`: registry, executor, hero, patch, OpenDota, STRATZ, and ranking tool entry points.
  - `planning/`: planner and output contract catalog.
  - `evidence/`: EvidenceGraph and extraction helpers.
  - `answer/`: answer synthesizer plus structured/natural entry points.
  - `critic/`: reviewer and critic rules entry points.
- Kept `graph.py`, `state.py`, and `models.py` at the `agentic/` top level for the LangGraph runner, shared state, and common models.
- Split `ToolExecutor` into `agentic/tools/executor.py`; `ToolRegistry` and `ToolDefinition` stay in `agentic/tools/registry.py`.
- Moved the hero resolver into `agentic/tools/hero_tools.py` and fixed the hero constants path.
- Updated application, scripts, and tests to import from the new structure.
- Updated architecture and V3 inventory documentation for the new agentic layout.

## Boundaries

- Did not change the `/api/v1/plan` public response shape.
- Did not add agentic tools or output contracts.
- Did not restore the old report/query path.
- Did not modify `apps/web`.

## Verification

- `python -m pytest tests/test_agentic_graph.py tests/test_agentic_nodes.py tests/test_plan_service.py tests/test_plan_route.py tests/test_plan_console.py`
  - Result: 24 passed.
- `python -m pytest`
  - Result: 139 passed.
- `python -m ruff check .`
  - Result: All checks passed.

