# DotaMind 进度快照：2026-07-18 10:30

## 会话记忆复审修订

- `player_profile_evidence()` 现在只读取 handler 输出的
  `confirmed_steam_account_id`；player tool 测试覆盖完整的
  handler → `ToolResult` → evidence 链路。
- stateful 安全失败写入专用脱敏 `Turn`：仅保存当前 query、粗粒度 status、稳定
  summary 与 `response_type="session_request_failed"`。被拒绝的 plan、模型
  reason、校验输入、context 和历史 sentinel 都不会成为后续会话记忆。
- `SessionStore.append()` 现在要求当前 task 持有
  `transaction(session_id)`。这明确了 get → run → append 的原子契约，并防止
  活跃 session 占满容量时，裸写入新 session 后立即被 LRU 淘汰。
- planner prompt 已明确：`stratz.player_recent_matches` 和
  `stratz.player_hero_performance` 之前必须先调用 `stratz.player_profile`，
  下游参数必须引用已确认的 Steam32 输出。

## 验证

- session store、privacy、player tool 与 plan service 聚焦测试：`44 passed`。
- 完整 API 测试：`332 passed, 1 warning`（FastAPI/Starlette 上游 `httpx`
  弃用提示）。
- `ruff check .` 与 `git diff --check` 通过。
