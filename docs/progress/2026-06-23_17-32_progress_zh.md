# MetaMind 进度快照：2026-06-23 17:32

> 本文记录 Dota2 英雄本地映射表的中文常用别称补充。

## 本次同步

- 扩充 `apps/api/app/data/heroes/dota2_heroes.yaml` 中的 `aliases` 字段。
- 补充中文互联网常见叫法、旧 Dota 中文名、社区简称和部分英文缩写，例如 `剑圣`、`屠夫`、`小鱼人`、`老奶奶`、`白虎`、`水人`、`卡尔`、`黑鸟` 等。
- 保持现有 YAML 单行数组格式，未修改英雄 `id`、OpenDota `name` 或 `localized_name`。
- 更新 `apps/api/tests/test_agentic_hero_resolver.py`，增加新增中文别称的默认 resolver 覆盖。

## 说明

- 本次只改本地英雄解析数据和对应测试，未修改 OpenDota、STRATZ 或报告 pipeline 逻辑。
- `es` 仍按既有行为保留为 `Earthshaker` 和 `Earth Spirit` 的歧义别称；现有测试覆盖该歧义解析。
- 已避免加入过短或过泛化的中文别称，减少误命中风险。

## 验证

- `python -c "from app.agentic.hero_resolver import load_hero_constants, HERO_CONSTANTS_PATH; heroes=load_hero_constants(HERO_CONSTANTS_PATH); print(len(heroes)); print(sum(len(h.aliases) for h in heroes))"`
- `python -m pytest tests/test_agentic_hero_resolver.py -q`
