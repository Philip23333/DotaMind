# DotaMind 进度快照：2026-07-06 17:15

> 本文记录 STRATZ 工具 P0 正确性收敛（审计文档 §4 P0-1/P0-2/P0-3）。目标是在动 P1 新能力前，把胜率口径、排序归属、排序键三类正确性问题收敛清楚。来源：`docs/design/STRATZ工具审计与重构输入.md` §4 + 计划 `enchanted-humming-floyd.md`。

## 修改总结

- **P0-2 排序归位（Commit 1）**：`_normalize_matchup_side` 删除内部 `sort`，集成层回归薄 relay（只 normalize 字段名），保留 STRATZ 原始迭代顺序；排序/topK 全部归 agentic 层 `_filter_matchup_rows`（按 synergy desc + match_count tie-break，原本就在此）。
- **P0-3 删死字段 + 去 over-fetch（Commit 1）**：
  - 删除从未透传到 evidence 的 `target_win_rate` / `hero_win_rate`（= provider 原生 `winRateHeroId1/2`）。
  - 同步从 `_HERO_VS_HERO_MATCHUP_QUERY` 两处 `vs {}` 块移除 `winRateHeroId1` / `winRateHeroId2`，消除 over-fetch。
  - 排序键固定为 `synergy`（STRATZ 合成 matchup 优势分，含样本加权），不做 `selection_mode`（matchup 已有 advantage/disadvantage 天然分组）。
- **P0-1 胜率口径显式命名（Commit 2）**：
  - matchup 的本地 `win_rate` → `matchup_win_rate`（明确「目标英雄对该对手的对局胜率 = winCount/matchCount」），与 lane 的 `match_win_rate`（match 级）区分。
  - 三条胜率 evidence（pair_lane_winrate / lane_meta_row / matchup_ranking_row）的 `value` 统一加 `win_rate_basis` 标签，**顶层 + `filters` 双写**，确保 answer LLM（只见 evidence graph）能正确措辞。
  - `hero_matchup_ranking` 与 `lane_meta_global` 的 description 补口径说明（planner 可见契约）。

## 关键发现 / 修正

- 审计文档 §2.2 原写「本地 `win_rate` 基本是死字段」**不准确**：核实后 `win_rate` 会进 evidence（`hero_matchup_ranking_evidence` 透传），非死字段，只是命名太泛；真正从未透传的死字段是 `target_win_rate` / `hero_win_rate`。已据此修正审计文档 §2.2 与 §4 P0-3 措辞。
- `win_rate_basis` 放置口径写死为「顶层为主 + filters 镜像」双写：顶层描述数值字段本身口径，filters 镜像守 provenance-in-filters 约定（memory `answer-sees-evidence-graph-only`）。

## 验证

- `pytest tests/test_stratz_heroes.py tests/test_agentic_stratz_tools.py tests/test_agentic_evidence.py tests/test_agentic_graph.py tests/test_agentic_answer.py tests/test_plan_service.py tests/test_agentic_critic.py` → 50 passed。
- 新增 `test_stratz_heroes_matchup_preserves_stratz_order`：2 条 advantage rows 故意逆 synergy 顺序，断言集成层保留 STRATZ 原始顺序（守护「排序发生在 agentic 层」契约）。
- `FakeTransport` 记录 `last_query`，断言 GraphQL 查询文本不含 `winRateHeroId1/2`（去 over-fetch 被测试覆盖）。
- `test_agentic_evidence.py` 加 `win_rate_basis` 顶层 + filters 双写断言。
- 验收 grep（限定 stratz 范围）：
  - `target_win_rate|hero_win_rate` in `apps/api/app` → 无。
  - `winRateHeroId` in `heroes.py` → 无。
  - `"win_rate"` in `apps/api/app/integrations/stratz` + `stratz_tools.py` → 无（只剩 `match_win_rate` / `matchup_win_rate`）。

## 边界

- 未做 P1：winDay 趋势工具、`hero_position_stats` 补 `winCount`、laneOutcome 补 stomp/cs、matchup 补 `with` 子查询。
- 未做 P2：per-week 扇出 helper、`region_ids/game_mode_ids` 对齐、position 透传统一、`matchLimit` 服务端过滤、selection_policy 跨工具统一。
- do-not-break 全部保留：`match_win_rate = matchWinCount/matchCount`（match 级）未改；week 窗口逻辑未碰；薄 relay 未发明聚合分。
